# Whitepapers

This folder stores ChainTrust Journal publication records for `whitepapers`.

Use this structure:

```text
whitepapers/YYYY/MM/safe-publication-slug/
```

Each record folder should contain the certified PDF, SHA-256 checksum, metadata file, KAS audit log entry, and signed ZIP package.
