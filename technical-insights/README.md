# Technical Insights

This folder stores ChainTrust Journal publication records for `technical-insights`.

Use this structure:

```text
technical-insights/YYYY/MM/safe-publication-slug/
```

Each record folder should contain the certified PDF, SHA-256 checksum, metadata file, KAS audit log entry, and signed ZIP package.
