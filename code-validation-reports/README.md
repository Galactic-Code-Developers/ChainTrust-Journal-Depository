# Code Validation Reports

This folder stores ChainTrust Journal publication records for `code-validation-reports`.

Use this structure:

```text
code-validation-reports/YYYY/MM/safe-publication-slug/
```

Each record folder should contain the certified PDF, SHA-256 checksum, metadata file, KAS audit log entry, and signed ZIP package.
