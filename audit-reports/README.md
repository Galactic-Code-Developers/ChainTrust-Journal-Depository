# Audit Reports

This folder stores ChainTrust Journal publication records for `audit-reports`.

Use this structure:

```text
audit-reports/YYYY/MM/safe-publication-slug/
```

Each record folder should contain the certified PDF, SHA-256 checksum, metadata file, KAS audit log entry, and signed ZIP package.
