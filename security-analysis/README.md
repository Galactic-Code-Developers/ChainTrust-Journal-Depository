# Security Analysis

This folder stores ChainTrust Journal publication records for `security-analysis`.

Use this structure:

```text
security-analysis/YYYY/MM/safe-publication-slug/
```

Each record folder should contain the certified PDF, SHA-256 checksum, metadata file, KAS audit log entry, and signed ZIP package.
